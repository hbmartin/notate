# Store accepted handwriting recognition in the Notebook

Accepted Transcriptions are editable, authoritative Notebook data stored with stable source-stroke links and provider provenance, while rejected candidates and debug diagnostics remain device-local. This makes search, retry, sync, and searchable PDF export agree on one user-controlled value instead of treating recognition as a disposable derived cache.
